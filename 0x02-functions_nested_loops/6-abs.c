#include "main.h"
/**
 * _abs - computes absolute value
 *
 * @c: input parameter
 *
 * Return: c if c is greater than zero -1*c else
 *
 */
int _abs(int c)
{
if (c >= 0)
{
return (c);
}
else
{
return (-1 * c);
}
}
